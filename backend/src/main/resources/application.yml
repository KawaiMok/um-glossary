server:
  port: 8080

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

app:
  data:
    glossary-json-path: data/glossary.json
    backup-dir: data/backups
    target-sheet: UM Title
